# 座位预约小程序 · 开发记录

> 校园自习室 / 图书馆「座位预约」微信小程序（个人主体，类目：工具-效率工具）
> 全栈 solo 项目：uni-app 前端 + Spring Boot 后端
> 记录人：小光 + AI 顾问协作；本文档随仓库提交，作为开发过程留档

---

## 〇、技术栈

| 层 | 选型 | 版本 |
|---|---|---|
| 前端 | uni-app（vite-ts 创建）+ Vue3 + TypeScript + Pinia + uni-ui | — |
| 包管理 | pnpm | 11.x |
| 后端 | Spring Boot | 3.5.7 |
| ORM | MyBatis-Plus（`mybatis-plus-spring-boot3-starter`） | 3.5.9 |
| 数据库 | MySQL | 8.0 |
| 鉴权 | JWT（jjwt） | 0.12.6 |
| 构建 | Maven（本机用 `mvn.cmd`，因 Git Bash 下 `mvn` 有路径空格坑） | 3.9.x |

> ⚠️ **Spring Boot starter 命名坑**：`spring-boot-starter-webmvc` / `webmvc-test` 是 **Spring Boot 4.x** 的命名；3.5.x 必须用 `spring-boot-starter-web` + `spring-boot-starter-test`（版本由 parent BOM 管理，不写 `<version>`）。误用 webmvc 会报 "Could not find artifact"。

---

## 一、阶段 1：最小闭环（建表 → 后端接口 → 前端列表）

**目标**：真机/页面看到后端真实数据，打通「数据库 → 接口 → 前端」链路。

**数据库**
- `t_venue(venue_id PK auto, venue_name, address)`
- `t_area(area_id PK auto, venue_id bigint not null, area_name, floor)`
- 主键列统一带 `_id` 后缀（后续实体必须注意驼峰映射）

**后端**
- `entity/Area.java`、`mapper/AreaMapper.java`、`controller/AreaController.java`（/api/area/list）
- 主类加 `@MapperScan("com.seat.mapper")`
- **踩坑**：① 文件夹误拼 `entiy` → 改名 `entity`；② `t_area` 主键列是 `area_id` 不是 `id` → 实体用 `areaId` + `@TableId(type=IdType.AUTO)`；③ `venue_id` 是 bigint → `venueId` 用 `Long`（误写 String 会映射失败）

**前端**
- `src/utils/request.ts`：封装 `uni.request`，`BASE_URL=http://localhost:8080`，自动带 JWT（阶段2补）
- 区域列表页调用 `/api/area/list` 展示 3 个区域
- **踩坑**：浏览器直连后端报 CORS → 自建 `config/CorsConfig.java` 放行 `http://localhost:5173`（H5 调试用）

**结果**：✅ 阶段1通关，真实数据链路跑通。

---

## 二、阶段 2：账号与鉴权（微信登录 + JWT）

**目标**：微信一键登录 → 后端用 code 换 openid → 落库用户 → 签发 JWT → 前端后续请求自动带 token → 拦截器校验。

**数据库**
- `t_user(user_id PK auto, open_id unique, student_no, nick_name, avatar_url, credit_score, create_time)`

**后端文件职责**
| 文件 | 职责 |
|---|---|
| `entity/User.java` | Lombok `@Data` + `@TableName("t_user")` |
| `mapper/UserMapper.java` | 含 `@Select("SELECT * FROM t_user WHERE open_id=#{openid}") User selectByOpenId(String openid)` |
| `service/WxService.java` | `code2Session` 调微信接口换 openid（**永远 HTTP 200**，错误在 body 的 `errcode`，需手动判错） |
| `controller/AuthController.java` | `POST /api/auth/login`：code → openid → 查/插用户 → 返 JWT |
| `util/JwtUtil.java` | jjwt 0.12.6 **静态工具类**（generate / getSubject），**不能 @Autowired，直接 `JwtUtil.generate`** |
| `interceptor/JwtInterceptor.java` | 取 `Authorization: Bearer <token>` 校验，失败返 401，成功 `setAttribute("openid")` |
| `config/WebMvcConfig.java` | 注册拦截器，拦 `/api/**`，放行 `/api/auth/login` |

**前端**
- `src/pages/login/login.vue`：启动页第一项。`uni.login({provider:'weixin'})` 拿 code → `request('/api/auth/login','POST',{code})` 拿 token → 存 `uni.setStorageSync('token')` → 跳首页
- `request.ts` 升级：有 token 时自动加 `Authorization: Bearer <token>`；收到 401 清 token 并提示「登录已过期」
- **小程序运行方式**：CLI 创建的 vite 项目 HBuilderX 识别不出是 uni-app，「运行到小程序模拟器」报"本项目类型不支持"。改用命令行 `pnpm dev:mp-weixin` 编译到 `frontend/unpackage/dist/dev/mp-weixin/`，再用**微信开发者工具**手动导入该目录联调。

**阶段2关键坑（按出现顺序）**
1. **AuthController 误用 `selectById(openid)`**：openid 是字符串，查 `user_id` 数字主键永远查不到 → 误判新用户 → 重复 insert → 触发 `open_id` 唯一索引冲突 `DuplicateKeyException` → 500。**第一次登录成功、第二次才炸**。必须改用 `selectByOpenId(openid)`。
2. **JwtUtil 是静态工具类**：不能注入，直接 `JwtUtil.generate(openid)`。
3. **jjwt 0.12 API 变更**：`signWith/verifyWith/parseSignedClaims`，Key 用 `SecretKey`（不再是旧版 `Key`）。
4. **application.yaml 混了 TAB**：YAML 不允许 TAB，启动报解析错误 → 改为空格（缩进 2 空格）。
5. **WorkBuddy 预览端口劫持**：工具自带 57082 端口抢占了预期端口，表现为启动异常；解决——脱离工具，在 **PowerShell** 里直接 `mvn.cmd spring-boot:run` 绑 8080。
6. **前端 401 是拦截器正常工作**：未带 token 直访 `/api/area/list` 返 401，不是 CORS 问题。
7. **三个 AppID 必须一致**：`manifest.json` 的 `mp-weixin.appid`（顶层 `appid` 是 DCloud 应用标识，填错地方无效）、微信开发者工具导入时填的 AppID、后端 `wx.appid/secret`（须换真实小程序凭证，当前是占位符）。

**崩溃恢复（2026-09-04 13:15）**
电脑掉 wifi 重启，编辑器未保存缓冲区丢失 6 个阶段2文件（User/UserMapper/WxService/AuthController/JwtInterceptor/WebMvcConfig）。经用户授权，AI 直接补全这 6 个文件（属丢失文件恢复，非替代写新功能），`mvn compile` 零报错，`t_user` 表仍在库。
> 📌 **教训**：每建好一个文件就 `git commit`，别等整段做完再提交，避免重启丢缓冲区。

**结果**：✅ 阶段2通关，微信登录 + JWT 全闭环联调通过。

---

## 三、当前进度与下一步

- ✅ 阶段1 最小闭环（区域列表真实数据）
- ✅ 阶段2 账号与鉴权（微信登录 + JWT）
- ⏭ 待办：`manifest.json` 填真实 `mp-weixin.appid` + `requiredPrivateInfos` + 微信后台《隐私保护指引》配置（个人主体微信登录须补隐私协议）
- ⏭ 后续阶段：座位查询/预约/签到/信用分等业务（按手册 02→06 纵向打通）

---

## 四、本机如何运行

**后端**（PowerShell，非 Git Bash）
```powershell
cd D:\微信小程序\seat-booking-app\backend
mvn.cmd spring-boot:run        # 绑定 8080
```

**前端**（编译到微信小程序目录）
```powershell
cd D:\微信小程序\seat-booking-app\frontend
pnpm dev:mp-weixin             # 产出 unpackage/dist/dev/mp-weixin/
```
用**微信开发者工具**导入 `frontend/unpackage/dist/dev/mp-weixin/` 目录联调。

**注意**：`backend/src/main/resources/application.yaml` 被 `.gitignore` 忽略（含数据库密码、微信 secret），不会进仓库。换机器需自行补该文件；如需共享配置模板，可另建 `application.example.yaml`（不提交真实密钥）。
